---
import { fetchThemes } from '../../data/themes.ts';
import Layout from '../../layouts/Themes.astro';

const themes: App.Theme[] = await fetchThemes();

const meta = {
    title: 'Themes',
    description: 'Start your next Astro project with a real world theme build by our amazing community.'
};
---

<Layout meta={meta} themes={themes} />