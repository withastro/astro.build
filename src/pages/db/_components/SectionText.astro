---
const { isCentered = false } = Astro.props
---

<div class:list={["space-y-1", isCentered && "sm:landing-section"]}>
	<h2
		class:list={[
			"text-balance body w-fit bg-red-pink-gradient bg-clip-text font-semibold uppercase tracking-widest text-transparent",
		]}
	>
		<slot name="eyebrow" />
	</h2>
	<h3 class:list={["text-balance heading heading-3 max-w-3xl font-bold"]}>
		<slot name="title" />
	</h3>
	{
		Astro.slots.has("default") && (
			<div class="body max-w-prose pt-4 text-astro-gray-200">
				<slot />
			</div>
		)
	}
</div>
