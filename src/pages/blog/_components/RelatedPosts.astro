---
import BlogLinkCard from './BlogLinkCard.astro';

interface Props {
	posts: Array<string>;
}
---

<div class="my-6 grid gap-8 grid-cols-[repeat(auto-fit,minmax(min(100%,18rem),1fr))]">
	{Astro.props.posts.map((id) => <BlogLinkCard {id} />)}
</div>
