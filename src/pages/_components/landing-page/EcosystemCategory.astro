---
interface Props {
  tab: string;
  slug?: string
  class?: string;
}

const { tab, class: className, slug } = Astro.props;
---

<li id={slug ? slug: tab} class:list={[className]} data-tab={tab}>
  <slot />
</li>
