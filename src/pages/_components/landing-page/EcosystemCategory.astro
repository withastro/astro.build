---
type Props = {
  id: string
  tab: string
  class?: string
}

const { id, tab, class: className } = Astro.props
---

<li
  data-tab={tab}
  role='tabpanel'
  id={`panel-${id}`}
  aria-labelledby={tab}
  class:list={[className]}
>
  <slot />
</li>
