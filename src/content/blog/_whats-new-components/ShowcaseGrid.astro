---
// import ShowcaseCard from './ShowcaseCard.astro';
import type { Props as CardProps } from './ShowcaseCard.astro';

interface Props {
	items: CardProps[];
}

const { items } = Astro.props;
---

{
	/*
<!-- Commented out for now due to flaky 11ty screenshot API -->
<ul
 class="grid gap-5 !p-0 md:grid-flow-row-dense md:grid-cols-2 lg:-mx-16 lg:!mb-16 lg:!mt-8 lg:grid-cols-3 xl:-mx-32"
>
  {items.map(({ url, title }) => <ShowcaseCard {url} {title} />)}
</ul>
	*/
}

<ul>
	{
		items.map(({ url, title }) => (
			<li>
				<a href={url}>{title}</a>
			</li>
		))
	}
</ul>
