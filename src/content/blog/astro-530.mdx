---
title: 'Astro 5.3'
description: 'Astro 5.3 is here with faster page rendering and easier setup for experimental sessions'
homepageLink:
  title: 'Astro 5.3'
  subtitle: 'Available now!'
publishDate: '2025-02-13'
authors:
  - matt
coverImage: '/src/content/blog/_images/astro-530/blog-post-5-3.webp'
socialImage: '/src/content/blog/_images/astro-530/og-astro-5-3.webp'
lang: 'en'
---

import BlogContentImage from '/src/components/BlogContentImage.astro';

**Astro 5.3 is here and it's faster than ever**

💘 It's the season of love, and you're sure to fall for these improvements to Astro:

- [**Faster page rendering**](#faster-page-rendering)
- [**Automatic session storage setup**](#automatic-session-storage-setup)

To upgrade an existing project, use the automated `@astrojs/upgrade` CLI tool. Alternatively, upgrade manually by running the upgrade command for your package manager:

```sh
# Recommended:
npx @astrojs/upgrade

# Manual:
npm install astro@latest
pnpm upgrade astro --latest
yarn upgrade astro --latest
```

## Faster page rendering

Astro makes it easy to use async functions inside components, but previously this meant that every component and page was rendered asynchronously, even if it wasn't needed. When this was happening for every component on a page, the overhead from these promises would really add up. Now, thanks to an awesome community contribution from [MatthewLymer](https://github.com/MatthewLymer), Astro will only render components asynchronously if they actually contain an async function call. In benchmarks, this has resulted in a significant speedup for rendering synchronous components. This benefits both on-demand and static builds, though the biggest improvements are seen in SSR response times. This even helps pages that use async functions, because most components on the page will still be synchronous.

The exact amount of the improvement will depend on the individual sites. Complex pages with many components will see the biggest improvements, while pages that use slower external APIs at request time may be unchanged. In our testing, SSR responses are generally 1.5-2x faster unless the page is querying external APIs. Some examples:

- For the 5300-page [Astro docs site](https://docs.astro.build), it **cut static build times by 10-15%.** 
- In the Platformic SSR benchmark, which generates a page with over 3000 components, requests are **4x faster**.

You don't need to do anything to benefit from this improvement: just sit back and enjoy the faster rendering times.

## Automatic session storage setup

Astro 5.1 introduced the experimental [session storage API](https://docs.astro.build/en/reference/experimental-flags/sessions/), which allows developers to easily manage user sessions on the server. The feature supports dozens of different backend storage drivers, powered by [unstorage](https://unstorage.unjs.io). Previously you would need to configure the session storage driver in your `astro.config.mjs`, but now Astro will automatically set up the session storage for you when using a supported adapter. 

Currently, these supported adapters include `@astrojs/node`, which uses the filesystem for storage, and `@astrojs/netlify`, which uses Netlify Blobs. We hope to add support for more adapters in the near future, where the hosting platforms include a storage API. In all cases, you can still manually configure your own session storage driver if you prefer.

To enable session storage, set the `experimental.session` flag in your `astro.config.mjs`:

```js title="astro.config.mjs" ins={6}
  {
    adapter: node({
      mode: "standalone",
    }),
    experimental: {
      session: true,
    },
  }
```

If you are already using the session feature, note that the syntax for the config has changed. The `experimental.session` option is now a boolean flag, and the actual session config has moved to a top-level `session` key. This allows you to enable the feature without needing to configure the session storage driver when on a platform that configures it for you.

```js title="astro.config.mjs" ins={9,11-13} del={6-8}
  {
    adapter: node({
      mode: "standalone",
    }),
    experimental: {
        session: {
            driver: "upstash",
        },
        session: true,
    },
    session: {
        driver: "upstash",
    },
  }
```

## Bug fixes

As always we've been working hard on fixing issues since the [5.2 release](/blog/astro-520). See [the changelog](https://github.com/withastro/astro/blob/main/packages/astro/CHANGELOG.md) for all the details.

## Thanks

Thanks to everyone who contributed to this release, including [Emanuele Stoppa](https://github.com/ematipico), [Sarah Rainsberger](https://github.com/sarah11918), [HiDeoo](https://github.com/HiDeoo), [Yan Thomas](https://github.com/yanthomasdev), [unprintable123](https://github.com/unprintable123), [Hacksore](https://github.com/Hacksore) and many more. Extra thanks to [MatthewLymer](https://github.com/MatthewLymer) for the excellent work on the faster rendering improvements.

We look forward to seeing what you build with Astro 5.3! If you have questions, comments, or just want to say hi, drop by the [Astro Discord](https://astro.build/chat).