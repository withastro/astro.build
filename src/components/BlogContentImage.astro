---
import { ImageMetadata } from "@astrojs/image/dist/vite-plugin-astro-image.js"

export interface Props {
	src: ImageMetadata
	alt?: string
}

const { src, alt = "" } = Astro.props as Props
---

<img
	src={src.src}
	alt={alt}
	width={src.width}
	height={src.height}
	class="mx-auto my-10 max-w-full rounded-xl shadow-lg"
	loading="lazy"
	decoding="async"
/>
