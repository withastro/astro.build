---
const { class: className, ...props } = Astro.props;
---
<section class={className} {...props}>
    <slot />
</section>

<style>
    section {
        background: var(--background, var(--color-tan));
        padding: 0 0 calc(var(--size-1000) * 1.5) 0;
    }
</style>
