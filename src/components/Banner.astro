---
const { href, items = [] } = Astro.props;
const Tag = href ? 'a' : 'div';
---

<not-marquee aria-hidden="true">
    <input id="marquee-pause" type="checkbox">
    <Tag {href} class="track">
        {Array.from({ length: 6 }, (_, i) => <span class="group" style={`--i: ${i};`}>{items.map(item => <span>{item}</span>)}</span>)}
    </Tag>
</not-marquee>
